[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-autopsy"
version = "0.1.0"
description = "A diagnostic and evaluation platform for understanding why RAG systems fail."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "sentence-transformers>=5.7,<6",
]

[project.scripts]
rag-autopsy = "rag_autopsy.cli:main"


[project.optional-dependencies]
dev = ["pytest", "ruff"]
postgres = ["psycopg[binary]>=3,<4", "pgvector>=0.4,<1"]
openai = ["openai>=1,<3"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
